"""NAE Reference corpus ingestion pipeline (TSU-separated).

Separate from `NAE.pipeline.index` (TSU) and `NAE.pipeline.ingest` (TSU
incremental).  Used only for reference corpora (dictionaries, encyclopedias,
etc.) that bypass the TSU claim extraction pipeline entirely.
"""
