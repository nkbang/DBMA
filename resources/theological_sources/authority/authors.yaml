# NAE Authority Registry — Authors (TEMPLATE — 실제 데이터 없음)
#
# NAE-SCHEMA-MIGRATION-001: 이 파일은 빈 템플릿이다. 실제 저자 데이터는
# 여기에 입력하지 않는다(금지 사항). 구조 검증 목적으로 example만 주석
# 형태로 남긴다 — sources: 배열은 비어 있다.
#
# 정본 스키마 설명: NAE_METADATA_GOVERNANCE_v1.md §5, ADR-016
# 실증 참고(구조 검증 완료, 프로덕션 데이터 아님):
#   resources/theological_sources/authority/pilot/authors.yaml
#   resources/theological_sources/authority/pilot/fuller/authors.yaml
#
# 필드 정의:
#   author_id       string, required  — canonical ID, 생성 규칙: NAE_CORPUS_INGESTION_STANDARD_v1.md "ID 생성 규칙"
#   canonical_name  string, required  — 정식 표기
#   aliases         array[string]     — 표기 변형(동일 인물로 확인된 것만, 자동 병합 금지)
#   birth_year      integer, optional
#   death_year      integer, optional
#   tradition       string, optional
#   domain          string, optional  — baptist(NAE-PD) | modern(NAE-MODERN), 검색 필터용
#   notes           string, optional
#
# 예시(구조 참고용, 데이터 아님):
#   authors:
#     - author_id: example_author
#       canonical_name: "Example Author"
#       aliases: ["Example A.", "A. Example"]
#       birth_year: null
#       death_year: null
#       tradition: null
#       domain: null
#       notes: null

schema_version: "1.0"
authors: []
