from pathlib import Path

ROOT = Path('/Users/David/DBMA')
FILES = {
    'CLAUDE.md': """# CLAUDE.md

## DBMA 프로젝트 개요
DBMA는 신학 문서 전용 RAG 시스템이다. Python 기반으로 다양한 문서 형식을 처리하고, 추출, 정제, 청킹, 임베딩, 검색, 생성, 평가를 하나의 흐름으로 연결한다. 핵심 목표는 결과 정밀도, 추적 가능성, 유지보수성, 그리고 반복 개선이다.

DBMA의 공식 실행 진입점은 `dbma_ui.py`(→ `ui/app.py`)이며, Streamlit UI와 전체 처리 흐름을 오케스트레이션한다. 과거 legacy application entry였던 `dbma.py`는 2026-07-17 커밋 `ce6b05a`로 `archive/legacy/`로 이동(격리)되어 현재 프로젝트 루트에는 존재하지 않는다(ADR-001, ADR-003, `docs/architecture/DBMA-Legacy-Code-Removal-Plan-v1.md` 참고). 프로젝트 루트는 `~/DBMA` 이다.

---

## 프로젝트 원칙
- DBMA는 신학 문서용 RAG 시스템이다.
- 결과 정밀도와 안정성을 우선한다.
- 작은 단위로 수정하고, 바로 검증한다.
- 작업은 반드시 추적 가능해야 한다.
- 로그는 읽기 쉽게 남긴다.
- md 파일을 사용해 상태와 과정을 문서화한다.
- `dbma_ui.py`(→ `ui/app.py`)를 시작점으로 연결 구조를 이해한다.
- 필요한 경우 함수 단위로 블록 다이어그램을 만든다.

---

## 기술 스택
- 언어: Python
- RAG 스택: LlamaIndex
- 기본 임베딩 모델: `bge-m3:latest`
- 기본 chunk size: `1200`
- 기본 overlap: `200`
- UI: Streamlit
- 개발 방식: 터미널 중심
- 문서 관리: Markdown 중심
- 목표 환경: MacBook Pro Max M5, 128 GB RAM

---

## 디렉터리 기준
기준 경로:
- 프로젝트 루트: `~/DBMA`
- 핵심 파일: `/Users/David/DBMA/dbma_ui.py` (→ `/Users/David/DBMA/ui/app.py`)

주요 모듈:
- `dbma_ui.py`: 공식 진입점 (thin launcher → `ui/app.py`)
- `dbma.py`: 제거됨 — `archive/legacy/dbma.py`로 이동 (2026-07-17, 커밋 `ce6b05a`)
- `core/`: 추출, 처리, 파일, 청킹, 유틸리티
- `ui/`: 탭 기반 인터페이스
- `tests/`: 테스트 코드
- `docs/`: 문서화
- `loops/`: 루프 엔지니어링 관련 산출물
- `scripts/`: 실행 및 평가 스크립트

---

## 작업 방식
1. 문제를 짧게 정의한다.
2. 관련 파일과 함수만 확인한다.
3. 필요한 경우 먼저 코드를 읽는다.
4. 수정은 최소 범위로 한다.
5. 실행 후 검증한다.
6. 결과를 md 파일에 기록한다.
7. 다음 반복에서 개선한다.

---

## 코드 구조 원칙
- 한 파일은 한 책임을 가진다.
- 한 함수는 한 역할을 가진다.
- 관련 없는 파일은 건드리지 않는다.
- 복사본 파일은 기준으로 쓰지 않는다.
- 임시 파일과 백업 파일은 혼동하지 않는다.
- 새 기능은 기존 흐름을 깨지 않게 넣는다.

---

## 파이프라인 순서
```text
원본 문서
→ 추출
→ 정제
→ 청킹
→ 저장
→ 임베딩
→ 검색
→ 생성
→ 평가
```

---

## 문서화 규칙
- 진행 상태는 md 파일로 남긴다.
- TODO 목록에는 체크포인트와 진행률을 포함한다.
- 상태 기록은 짧고 읽기 쉽게 쓴다.
- 필요하면 파이프라인 문서와 설계 문서를 분리한다.
- 함수 연결 구조는 블록 다이어그램으로 정리한다.
- `dbma_ui.py`(→ `ui/app.py`)를 시작점으로 전체 연결을 문서화한다.

추천 문서 예시:
- `docs/ARCHITECTURE.md`
- `docs/PIPELINE.md`
- `docs/STATE.md`
- `docs/TODO.md`
- `docs/DBMA_MAP.md`

---

## 루프 엔지니어링 규칙
- 목표를 정하고 실행한다.
- 결과를 보고 수정한다.
- 다시 실행하고 비교한다.
- 이 과정을 안정될 때까지 반복한다.
- 실패는 기록하고 다음 시도에 반영한다.
- 진행률은 퍼센트로 표시한다.
- 가능한 경우 상태를 시각적으로 남긴다.

권장 항목:
- 목표
- 현재 상태
- 수정 내용
- 검증 결과
- 다음 조치
- 진행률

---

## 로그 규칙
- 로그는 읽기 쉬워야 한다.
- 처리 단계별로 핵심 로그를 남긴다.
- 원인과 결과를 구분해서 기록한다.
- 너무 길게 쓰지 않는다.
- 실패 시 재현 정보도 남긴다.

---

## UI 규칙
- UI는 탭 구조를 유지한다.
- Mac 친화적인 흐름을 우선한다.
- 처리 중 상태를 분명히 보여준다.
- 필요하면 다른 탭을 비활성화한다.
- 사용자가 현재 상태를 바로 알 수 있어야 한다.

---

## RAG 규칙
- LlamaIndex 기반으로 관리한다.
- 기본 임베딩은 `bge-m3:latest`를 사용한다.
- 기본 chunk size는 `1200`이다.
- 기본 overlap은 `200`이다.
- 문서별 특성에 따라 청킹은 조정 가능해야 한다.
- 헬라어와 히브리어 처리 가능성을 고려한다.
- 검색과 생성은 분리해서 다룬다.

---

## 테스트 규칙
- 변경 후 반드시 테스트한다.
- 테스트는 작은 단위부터 시작한다.
- 실패한 테스트는 바로 기록한다.
- 핵심 기능에는 회귀 방지 테스트를 붙인다.
- 가능하면 자동 테스트를 만든다.

---

## Cline 사용 규칙
- Cline은 보조 도구로만 사용한다.
- 한 번에 하나의 파일 또는 하나의 함수만 수정하게 한다.
- 복잡한 코드는 Claude 4.5 Sonnet 기준으로 다룬다.
- 불필요한 탐색과 넓은 수정은 피한다.
- 요청은 구체적으로 쓴다.
- 결과는 바로 검증 가능해야 한다.
- 디버깅은 가능하면 직접 확인한다.

---

## 파일 관리 규칙
- 복사본 파일은 참고용일 뿐 기준이 아니다.
- 최신 본문을 기준으로 판단한다.
- 이름이 비슷한 임시 파일은 정리 대상이다.
- 프로젝트 기준 경로는 항상 `~/DBMA`로 본다.

---

## 상태 관리 규칙
- 현재 진행 상황을 항상 파악할 수 있어야 한다.
- TODO는 완료, 진행 중, 대기 중으로 나눈다.
- 가능하면 진행률을 퍼센트로 표시한다.
- 큰 작업은 체크포인트로 나눈다.

---

## 금지 사항
- 근거 없는 구조 변경 금지.
- 한 번에 여러 큰 파일 무리하게 수정 금지.
- 복사본 파일을 기준으로 삼는 것 금지.
- 불필요하게 넓은 리팩터링 금지.
- 설명보다 실행 가능한 결과를 우선.

---

## 최종 기준
DBMA의 목적은 단순히 돌아가는 코드가 아니라, 신학 문서를 안정적으로 다루고, 문서 흐름을 추적 가능하게 유지하며, 반복 개선이 가능한 시스템을 만드는 것이다.
""",
    'docs/README.md': """# DBMA Documentation Overview

## 목적
이 문서는 DBMA 문서 전체의 간단한 소개와 사용 안내를 제공한다. 새로 들어온 사람이 문서 체계를 빠르게 이해할 수 있도록 돕는다.

---

## 문서 구성
- `CLAUDE.md`.
  - 프로젝트 운영 규칙과 개발 원칙.
- `docs/STYLE.md`.
  - 문서 작성 스타일.
- `docs/SCHEMA.md`.
  - 문서 기본 구조.
- `docs/ARCHITECTURE.md`.
  - 시스템 구조 설명.
- `docs/PIPELINE.md`.
  - 처리 흐름 설명.
- `docs/DBMA_MAP.md`.
  - 함수와 모듈 연결도.
- `docs/STATE.md`.
  - 현재 상태와 진행률.
- `docs/TODO.md`.
  - 해야 할 일 목록.
- `docs/PROCESS_LOG.md`.
  - 작업 기록.

---

## 사용 방법
1. 먼저 `CLAUDE.md`를 본다.
2. 문서 체계는 이 파일에서 확인한다.
3. 구조는 `docs/ARCHITECTURE.md`를 본다.
4. 흐름은 `docs/PIPELINE.md`를 본다.
5. 상태는 `docs/STATE.md`를 본다.
6. 작업 기록은 `docs/PROCESS_LOG.md`를 본다.
7. 연결 구조는 `docs/DBMA_MAP.md`를 본다.

---

## 문서화 원칙
- 문서는 짧고 분명해야 한다.
- 한 문서는 한 목적을 가진다.
- 구조, 흐름, 상태, 기록을 섞지 않는다.
- 바뀐 내용은 바로 반영한다.
- `dbma_ui.py`(→ `ui/app.py`)를 기준으로 설명한다.
""",
    'docs/STYLE.md': """# DBMA Writing Style

## 목적
이 문서는 DBMA 문서와 코드 설명을 쓸 때 지켜야 할 스타일 기준을 정리한다.

---

## 기본 원칙
- 짧고 명확하게 쓴다.
- 어려운 말보다 쉬운 말을 쓴다.
- 한 문장에는 한 가지 뜻만 담는다.
- 장황한 설명보다 바로 이해되는 표현을 쓴다.
- 실무에서 바로 쓸 수 있어야 한다.
""",
    'docs/SCHEMA.md': """# DBMA Schema

## 목적
이 문서는 DBMA 문서와 데이터 기록에서 자주 쓰는 구조를 정리한다.

---

## 문서 공통 구조
```md
# 제목

## 목적
## 개요
## 본문
## 비고
```
""",
    'docs/ARCHITECTURE.md': """# DBMA Architecture

## 목적
이 문서는 DBMA 프로젝트의 전체 구조를 한눈에 이해하기 위한 아키텍처 문서다.

---

## 시스템 개요
DBMA는 신학 문서 전용 RAG 시스템이다. 문서 추출, 정제, 청킹, 저장, 임베딩, 검색, 생성, 평가가 하나의 흐름으로 연결된다.
""",
    'docs/PIPELINE.md': """# DBMA Pipeline

## 목적
이 문서는 DBMA의 실제 처리 흐름을 단계별로 정리한 파이프라인 문서다.

---

## 파이프라인 개요
```text
입력
→ 추출
→ 정제
→ 청킹
→ 저장
→ 임베딩
→ 검색
→ 생성
→ 평가
→ 문서화
```
""",
    'docs/DBMA_MAP.md': """# DBMA Function Map

## 목적
이 문서는 `dbma_ui.py`(→ `ui/app.py`)를 시작점으로 DBMA 프로젝트의 연결 관계를 함수 단위로 정리한다.

---

## 전체 흐름
```text
dbma_ui.py
→ ui/app.py
→ ui/
→ core/
```
""",
    'docs/OPERATIONS.md': """# DBMA Operations

## 목적
이 문서는 DBMA 프로젝트를 실제로 운영할 때 필요한 기준을 정리한다.

---

## 운영 흐름
```text
개발
→ 테스트
→ 검증
→ 문서 반영
→ 배포 준비
→ 실행
→ 유지보수
```
""",
    'docs/TESTING.md': """# DBMA Testing

## 목적
이 문서는 DBMA 프로젝트를 테스트하는 기준과 방법을 정리한다.

---

## 테스트 원칙
- 테스트는 작고 분명해야 한다.
- 한 번에 하나씩 확인한다.
- 실패 원인을 바로 기록한다.
- 결과는 md 파일이나 로그로 남긴다.
- 가능한 한 자동화한다.
""",
    'docs/DEPLOYMENT.md': """# DBMA Deployment

## 목적
이 문서는 DBMA 프로젝트를 실행하고 배포하는 기준을 정리한다.

---

## 로컬 실행
```bash
cd ~/DBMA
source .venv/bin/activate
streamlit run dbma_ui.py
```
""",
    'docs/MAINTENANCE.md': """# DBMA Maintenance

## 목적
이 문서는 DBMA 프로젝트를 계속 안정적으로 유지하기 위한 점검 기준과 관리 방법을 정리한다.

---

## 유지보수 원칙
- 작은 수정부터 한다.
- 수정 후 바로 확인한다.
- 로그를 남긴다.
- 오래된 복사본은 기준으로 쓰지 않는다.
- 문서와 코드가 함께 맞아야 한다.
""",
    'docs/STATE.md': """# DBMA State

## 현재 상태
DBMA는 신학 문서 전용 RAG 시스템으로 개발 중이다.
""",
    'docs/TODO.md': """# DBMA TODO

## 현재 목표
DBMA의 문서 처리 파이프라인을 안정화하고, md 산출물 생성과 청킹 흐름을 정상화한다.
""",
    'docs/PROCESS_LOG.md': """# DBMA Process Log

## 목적
이 문서는 DBMA에서 수행한 작업의 흐름을 읽기 쉽게 남기기 위한 기록 파일이다.
""",
    'docs/CHANGELOG.md': """# DBMA Changelog

## 목적
이 문서는 DBMA 프로젝트의 중요한 변경 사항을 날짜별로 기록한다.
""",
}

def main():
    for rel, text in FILES.items():
        path = ROOT / rel
        path.parent.mkdir(parents=True, exist_ok=True)
        path.write_text(text, encoding='utf-8')
        print(path)

if __name__ == '__main__':
    main()