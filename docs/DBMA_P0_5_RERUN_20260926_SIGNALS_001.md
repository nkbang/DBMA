# P0-5 24건 재실행 객관 신호 보고 001 (2026-09-26)

- 작성: CUE · 일자: 2026-09-26
- 배경: 한국어 질의 검색 복구(`DBMA_KOREAN_QUERY_RETRIEVAL_FIX_REPORT_001.md`)로
  2026-09-23 채점(NOT VERIFIED 17/24)의 전제가 무효가 되어 원문 답변을 재수집
- 실행: `scripts/p0_5_run_all.py` (프로덕션 경로, k=5, file_scope=None)
- 09-23 기준선 보존: `docs/NAE_GOLD_QUERY_SET_P0_5_RAW_ANSWERS_001.20260923.bak.json`

> **이 문서는 채점이 아니다.** 질의 세트 문서가 채점을 임계 경로("사용자만 할 수
> 있다")로 규정하고, 09-23의 자동 판정은 **예외 승인**이었으며 재발 시 다시 명시적
> 승인이 필요하다고 기록돼 있다. 따라서 여기에는 PASS/FLAG/FAIL을 붙이지 않고
> **객관 신호와 09-23 대비 변화만** 기록한다.

---

## 1. 핵심 변화

| 신호 | 09-23 | 09-26 |
|---|---:|---:|
| 근거 0건 **하드 유보** | 17/24 | **0/24** |
| 인용 메타데이터 공백 | 0/105 | **0/360** |
| ClaimGuard 위험 표시 | — | 0건 |
| 생성 오류 | — | 0건 |

검색 복구의 효과가 그대로 나타났다 — 하드 게이트로 막혀 있던 17건이 전부 근거를
받아 생성 단계까지 도달했다.

## 2. "0/24 유보"를 오독하면 안 된다

`evidence_hold`는 **검색 0건 하드 게이트**만 센다. 근거를 받고도 답변 안에서
근거 부족을 인정한 건은 별도로 세야 한다.

| 구분 | 건수 | 태그 |
|---|---:|---|
| 답변 내 유보 | **10** | A2, B3, D2, E1, F1, F2, G1, G2, G3, H3 |
| 실제 답변 생성 | **14** | A1, A3, B1, B2, C1, C2, C3, D1, D3, E2, E3, F3, H1, H2 |

10건 중 **G1·G2·G3는 유보가 정답**이다(존재하지 않는 공식 입장·가짜 신학자·
코퍼스 밖 주제 — `DBMA_G2_HALLUCINATION_TRAP_RECHECK_001.md`에서 별도 검증).
나머지 7건은 근거를 받았으나 충분하지 않다고 모델이 판단한 경우다.

답변 길이: 최소 130자 / 중위 428자 / 최대 865자.

---

## 3. 새로 드러난 문제 — 문자 오염이 답변을 훼손한다

| 항목 | 값 |
|---|---:|
| 오염 탐지 이벤트 | **28회** |
| 재시도 소진 → **강제 삭제** | **7회** |

탐지된 문자 체계가 넓다 — 가나(`リ ス ト`, `べ て`, `た だ`), CJK(`義`, `法則`,
`離婚 姦淫`), 태국어(`ตาม`, `ยม`), 키릴(`процес`, `истор`).

### 3.1 강제 삭제는 문장을 망가뜨린다 (실증)

`_sanitize_script_contamination()`은 재시도를 다 쓰면 오염 문자를 **대체 없이
삭제**한다. E2 답변에 그 잔해가 남았다 — 같은 문구가 두 번 반복된다:

> …목사로서 성도의 행복과 믿음의 결실을 위해 기도하고 관심을 가져야 합니다
> **(C.H. 스펀지언은 "목사가"이라고 하지 않았으므로 이 부분에 대한 답변으로는
> 사용할 수 없습니다)**

문장이 성립하지 않는다. 오염 목록(`牧師`, `幸福`, `喜樂`, `神`)이 삭제되면서
남은 조각으로 보인다. 목회자가 그대로 읽을 수 없는 상태다.

### 3.2 반면 훼손되지 않은 답변도 있다 (E1)

> 이 질문은 현재 등록된 자료로는 답할 수 없습니다. … 그러나 Charles Haddon
> Spurgeon의 설교에서는 "saving for the cause of fornication"이라는 표현이
> 등장하며, 이는 마태복음 5장 32절에서 … 외도 경우를 제외하고는 이혼을
> 허용하지 않는다는 의미로 해석할 수 있습니다.

근거 부족을 먼저 밝히고, 실제로 찾은 것(마태복음 5:32 관련 표현)을 정확히
제시하고, 적용상 주의까지 덧붙였다 — 원하던 동작이다.

### 3.3 인명 표기 불일치

같은 인물이 **스펀지언 / 스페어전 / 스페전** 세 가지로 표기된다(09-23 채점에서도
지적된 항목). 영문 코퍼스의 고유명사를 한국어로 옮기는 과정의 문제다.

---

## 4. 원인 귀속 — 내 변경이 드러낸 것이지, 만든 것이 아니다(단서 있음)

오염 자체는 이전부터 있었다(`_SCRIPT_CONTAMINATION_RE`와 재시도 로직이 그 대응).
다만 09-23에는 **17/24가 생성 단계에 도달하지 못해** 오염이 발생할 기회가 없었다.
검색 복구로 24/24가 생성되면서 노출 빈도가 급증했다.

동시에 **번역 전처리가 다국어 압력을 키운 측면도 있다** — 영어로 번역된 질의 +
영문 근거로 한국어 답변을 만들게 하므로, 모델이 중간 언어 토큰을 섞을 여지가
커진다. 두 요인의 비중은 측정하지 않았으므로 단정하지 않는다.

**결론: 기존 결함이 이제 상시 노출된다.** 09-23에 "수정 보류"로 남긴
오염 필터 사각지대 항목을 **배포 차단급으로 올려야 한다** — 강제 삭제가 답변
본문을 훼손한 사례가 실증됐기 때문이다.

---

## 5. 후속 (권고)

1. **오염 대응 재설계** — 현재는 "재시도 3회 → 실패 시 삭제"다. 삭제가 문장을
   망가뜨리므로, 재시도 소진 시 **삭제 대신 유보**로 전환하는 편이 이 앱의
   원칙(없으면 없는 대로)에 맞다. 훼손된 답변보다 정직한 유보가 낫다.
2. 라틴 문자 사각지대(09-23 B1/B2 관찰: `persönlich`, `bahwa`)는 여전히 미해결.
3. 인명 표기 정규화 — 고유명사 음역 규칙 필요.
4. **채점** — 위 원문 24건에 대한 PASS/FLAG/FAIL은 사용자 몫으로 남는다.
   결과 파일: `docs/NAE_GOLD_QUERY_SET_P0_5_RAW_ANSWERS_001.json`

---

## 6. 기록

```
STATUS:      재수집 완료 (채점 아님)
Changed:     docs/NAE_GOLD_QUERY_SET_P0_5_RAW_ANSWERS_001.json (재생성)
             09-23 원문은 .20260923.bak.json 으로 보존
핵심:        하드 유보 17/24 → 0/24 · 답변 내 유보 10/24(트랩 3 포함) · 실제 답변 14/24
신규 문제:   문자 오염 28회 탐지 / 7회 강제 삭제 → 답변 훼손 실증(E2)
Next:        오염 대응 재설계(삭제→유보) · 채점(사용자)
```
